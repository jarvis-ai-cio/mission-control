# Convex deployment URL — get this from `npx convex dev`
NEXT_PUBLIC_CONVEX_URL=https://<your-deployment>.convex.cloud

# Jarvis Gateway — only needed for the /api/cron memory sync endpoint
# Leave blank for local development unless testing agent memory sync
OPENCLAW_GATEWAY_URL=https://jarvis.lemming-neon.ts.net
OPENCLAW_GATEWAY_TOKEN=

# better-auth
BETTER_AUTH_SECRET=    # generate: openssl rand -base64 32
BETTER_AUTH_URL=https://mission-control.iamchuka.com   # your app's public URL
AUTH_DB_PATH=/data/auth.db   # path to SQLite file (K8s PVC mounts /data)
